// The Forecast screen: ten days, one column each, to the left of home on
// the strip (swipe left from Today to reach it, right to come back).
#pragma once

#include "ui/screen_manager.h"

const ScreenDef &screen_forecast_def();
