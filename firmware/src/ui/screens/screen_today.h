#pragma once

#include "ui/screen_manager.h"

// The primary screen: current conditions on the left, the next ten hours on the
// right, and a temperature ribbon running behind the strip.
const ScreenDef &screen_today_def();
